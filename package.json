{
  "name": "anderseed-consulting-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "preview:assessment": "node scripts/assessment-preview-server.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@netlify/database": "^2.0.0"
  }
}
